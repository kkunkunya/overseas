# Skills domain

# 本项目当前不声明任何业务领域——知识库学习阶段纯靠 global skill
# （grilling / domain-modeling / research）。
#
# 未来进入第 2 步"建站开发"做产品时，在此追加领域，每行一个 kebab-case：
#   frontend      ← 建站做前端（project-design-prompt / anti-slop-frontend / design-recon）
#   engineering   ← 写业务逻辑（tdd / code-review / implement / diagnosing-bugs）
#   deploy        ← 上线部署（use-railway）
# 然后调用 /sync-project-skills 分发。